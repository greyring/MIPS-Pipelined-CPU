


// Instantiate the module
Status instance_name (
    .clk(clk), 
    .rst(rst), 
    .we(we), 
    .mtcd(mtcd), 
    .IM_(IM_), 
    .ERL_(ERL_), 
    .EXL_(EXL_), 
    .IE_(IE_), 
    .Q(Q)
    );


